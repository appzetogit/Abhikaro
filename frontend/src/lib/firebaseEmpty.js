// Stub module for bare 'firebase' imports from legacy third-party code.
// The app uses modular Firebase imports (firebase/app, firebase/auth, etc.)
// so this file should never be used at runtime. It only satisfies bundler
// resolution to avoid Vite/rollup errors.

export default {};

